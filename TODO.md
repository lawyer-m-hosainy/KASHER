# P2 — PRODUCT GAPS

The following items are remaining feature gaps and improvements to be implemented:

- [ ] **13. UI Enhancements**: Replace all `confirm()` and `alert()` calls with the existing unused `ConfirmModal.tsx` component.
- [ ] **14. CSV Export**: Add CSV export functionality for inventory and reports.
- [ ] **15. PWA Support**: Register a service worker using `vite-plugin-pwa` and add real icon files matching `manifest.json`.
- [ ] **16. Testing & CI**: Add `vitest` coverage for the checkout flow (both online and offline scenarios) and configure a GitHub Actions CI workflow to run `tsc` and tests on push.
